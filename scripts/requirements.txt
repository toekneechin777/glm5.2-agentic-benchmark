# Python deps for the RunPod monitoring scripts.
# Install on the RunPod instance:
#   pip install -r scripts/requirements.txt
#
# monitor_vram.py: pynvml is optional; --use-nvidia-smi needs only nvidia-smi.
# extract_ttft.py: requests is only needed for the --metrics-url mode.
nvidia-ml-py3>=7.352.0
requests>=2.31.0
